
import 'package:flutter/material.dart';

class SignBar extends StatefulWidget {

  static const routeName = '/categories';

  const SignBar({Key? key}) : super(key: key);

  @override
  State<SignBar> createState() => _SignBarState();
}

class _SignBarState extends State<SignBar> {
  @override
  Widget build(BuildContext context) {
    return Container(); // Add a return statement to fix the error
  }
  
  }